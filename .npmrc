# Expo's Metro bundler requires hoisted node_modules to resolve transitive
# dependencies across the pnpm virtual store. Without this, Metro can't find
# packages like @babel/runtime that aren't directly declared in mobile/package.json.
shamefully-hoist=true
